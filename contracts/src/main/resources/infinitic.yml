# Pulsar settings
pulsar:
  brokerServiceUrl: pulsar://localhost:6650/
  webServiceUrl: http://localhost:8080
  tenant: infinitic
  namespace: dev
  # brokerServiceUrl: pulsar+ssl://
  # webServiceUrl: https://
  # authentication:
  #   token:

# Storage settings
storage:
  # inMemory:
  redis:
  # mysql:
  # postgres
  #   host:
  #   port:
  #   user:
  #   password:
  #   database:

# Dashboard Settings
port: 9080
