plugins {
    id 'application'
}

application {
    // Define the main class for the application.
    mainClassName = 'com.acme.workflows.booking.java.Worker'
}

tasks.withType(JavaCompile) {
    options.compilerArgs << "-Xlint:unchecked"
}